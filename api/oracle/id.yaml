get:
  tags: [oracle]
  summary: Get identifying information about an identity oracle.

  responses:
    200:
      description: Successful operation
      content:
        application/json:
          schema:
            type: object
            properties:
              contract_address:
                type: string
                description: Bitcoin address of smart contract agent that defines the identity oracle.
              public_key:
                type: string
